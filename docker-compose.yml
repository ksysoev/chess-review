services:
  chess-review:
    build:
      context: .
      dockerfile: Dockerfile
    # Mount the current directory so any .pgn file in it is accessible
    # inside the container at /games/<filename>.
    # Usage: docker compose run --rm chess-review /games/my-game.pgn
    volumes:
      - ./games:/games:ro
